# Django hello world

Quickstart:
- Install deps once: `python -m pip install -r requirements.txt`
- Run dev server: `python manage.py runserver`
- Home page: http://127.0.0.1:8000/ returns `Hello, world!`
- Tests: `python manage.py test`
